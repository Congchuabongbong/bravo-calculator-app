import { InjectionToken } from '@angular/core';
export const INITIAL_STATE_CALCULATOR = new InjectionToken('init state for calculator');
export const RECEIVER_TOKEN = new InjectionToken('receiver token');
